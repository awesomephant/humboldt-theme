{% extends "base.twig" %}

{% block content %}
{% include "header.twig" %}
<article class="object post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
	<section class="object-details page-content">
		<h2>{{post.title}}</h2>
		{{post.content}}
	</section>
</article>
{% endblock %}